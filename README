Vehicle Detection with NVIDIA DeepStream
This project demonstrates vehicle detection and raise alert when any vehicle comes in defined roi using NVIDIA DeepStream. It uses GStreamer pipelines with the NVIDIA DeepStream SDK to process video streams, detect vehicles, and display the results.

Prerequisites
NVIDIA GPU with CUDA support
NVIDIA DeepStream SDK
GStreamer
